/**
 * Channel 抽象层 — re-export from @oagent/channels
 */
export type { Channel, ChannelStatus, ChannelMessageEvent, ChannelStartOpts } from '@oagent/channels';
export { ChannelManager, channelManager, SessionManager } from '@oagent/channels';
export type { ChannelState } from '@oagent/channels';
